# 🏗️ SQLite Integration Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    Campus Mentor App                         │
│                  SQLite Integration Layer                    │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   Frontend   │      │   Backend    │      │   Database   │
│              │      │              │      │              │
│  HTML/CSS/JS │─────▶│  Express.js  │─────▶│   SQLite3    │
│              │      │   Node.js    │      │              │
│ Test Page    │◀─────│  REST API    │◀─────│ campus_      │
│              │      │              │      │ mentor.db    │
└──────────────┘      └──────────────┘      └──────────────┘
     │                      │                      │
     │                      │                      │
     ▼                      ▼                      ▼
  Browser              Server.js              File System
```

---

## Data Flow

### 1. Add User Flow
```
User Input (Form)
    │
    ▼
POST /api/sqlite/users
    │
    ▼
Express Route Handler
    │
    ▼
SQLiteUser.addUser()
    │
    ▼
Database Query (INSERT)
    │
    ▼
campus_mentor.db
    │
    ▼
Return User Object
    │
    ▼
JSON Response
    │
    ▼
Display in UI
```

### 2. Get Users Flow
```
User Click "Get All Users"
    │
    ▼
GET /api/sqlite/users
    │
    ▼
Express Route Handler
    │
    ▼
SQLiteUser.getUsers()
    │
    ▼
Database Query (SELECT *)
    │
    ▼
campus_mentor.db
    │
    ▼
Return Users Array
    │
    ▼
JSON Response
    │
    ▼
Render User Cards
```

---

## File Structure

```
campus-mentor/
│
├── 📁 db/
│   └── database.js              ← Database initialization
│
├── 📁 models/
│   ├── SQLiteUser.js            ← User CRUD operations (NEW)
│   ├── User.js                  ← Firebase user model
│   ├── Achievement.js           ← Achievement model
│   └── TeacherSpecification.js  ← Teacher spec model
│
├── 📁 pages/
│   ├── sqlite-test.html         ← Interactive test UI (NEW)
│   └── [other pages...]
│
├── 📁 services/
│   ├── FirebaseService.js
│   ├── UserService.js
│   └── [other services...]
│
├── 📄 server.js                 ← Main server (UPDATED)
├── 📄 sqlite-demo.js            ← Full demo script (NEW)
├── 📄 test-sqlite.js            ← Quick test (NEW)
├── 📄 package.json              ← Dependencies (UPDATED)
│
├── 🗄️ campus_mentor.db          ← SQLite database (AUTO-CREATED)
│
└── 📚 Documentation/
    ├── README_SQLITE.md         ← Quick start guide
    ├── SQLITE_INTEGRATION.md    ← Technical docs
    ├── HACKATHON_DEMO.md       ← Presentation guide
    ├── SQLITE_SUMMARY.md       ← Complete summary
    └── ARCHITECTURE.md         ← This file
```

---

## Database Schema

```sql
┌─────────────────────────────────────────┐
│              users table                 │
├──────────┬──────────┬───────────────────┤
│  Column  │   Type   │   Constraints     │
├──────────┼──────────┼───────────────────┤
│ id       │ INTEGER  │ PRIMARY KEY       │
│          │          │ AUTOINCREMENT     │
├──────────┼──────────┼───────────────────┤
│ name     │ TEXT     │ NOT NULL          │
├──────────┼──────────┼───────────────────┤
│ email    │ TEXT     │ NOT NULL          │
│          │          │ UNIQUE            │
├──────────┼──────────┼───────────────────┤
│ role     │ TEXT     │ NOT NULL          │
│          │          │ CHECK(role IN     │
│          │          │ 'student',        │
│          │          │ 'teacher',        │
│          │          │ 'admin')          │
├──────────┼──────────┼───────────────────┤
│created_at│ DATETIME │ DEFAULT           │
│          │          │ CURRENT_TIMESTAMP │
└──────────┴──────────┴───────────────────┘
```

---

## API Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    REST API Layer                        │
│                /api/sqlite/*                             │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
   ┌─────────┐      ┌─────────┐      ┌─────────┐
   │  POST   │      │   GET   │      │   PUT   │
   │ /users  │      │ /users  │      │/users/:id│
   └─────────┘      └─────────┘      └─────────┘
        │                 │                 │
        ▼                 ▼                 ▼
   ┌─────────────────────────────────────────┐
   │         SQLiteUser Model                 │
   │  ┌────────────────────────────────┐     │
   │  │ addUser()                      │     │
   │  │ getUsers()                     │     │
   │  │ getUserById()                  │     │
   │  │ getUsersByRole()               │     │
   │  │ updateUser()                   │     │
   │  │ deleteUser()                   │     │
   │  │ searchUsers()                  │     │
   │  │ getUserStats()                 │     │
   │  └────────────────────────────────┘     │
   └─────────────────────────────────────────┘
                    │
                    ▼
        ┌───────────────────────┐
        │   SQLite Database     │
        │   campus_mentor.db    │
        └───────────────────────┘
```

---

## Component Interaction

```
┌──────────────────────────────────────────────────────────┐
│                    User Interface                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ Add Form │  │User List │  │ Search   │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
└───────┼─────────────┼─────────────┼────────────────────┘
        │             │             │
        │   HTTP      │   HTTP      │   HTTP
        │   POST      │   GET       │   GET
        │             │             │
        ▼             ▼             ▼
┌──────────────────────────────────────────────────────────┐
│                   Express Server                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │              Route Handlers                       │   │
│  │  app.post()  app.get()  app.put()  app.delete() │   │
│  └────┬──────────────┬──────────────┬────────────┬──┘   │
└───────┼──────────────┼──────────────┼────────────┼──────┘
        │              │              │            │
        ▼              ▼              ▼            ▼
┌──────────────────────────────────────────────────────────┐
│                   SQLiteUser Model                        │
│  ┌──────────────────────────────────────────────────┐   │
│  │         Database Operations Layer                 │   │
│  │  • Input Validation                              │   │
│  │  • SQL Query Construction                        │   │
│  │  • Error Handling                                │   │
│  │  • Result Formatting                             │   │
│  └────┬──────────────┬──────────────┬────────────┬──┘   │
└───────┼──────────────┼──────────────┼────────────┼──────┘
        │              │              │            │
        ▼              ▼              ▼            ▼
┌──────────────────────────────────────────────────────────┐
│                  SQLite Database                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │              campus_mentor.db                     │   │
│  │  ┌────────────────────────────────────────────┐  │   │
│  │  │           users table                      │  │   │
│  │  │  • id, name, email, role, created_at      │  │   │
│  │  └────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
```

---

## Request/Response Cycle

### Example: Adding a User

```
1. User fills form on sqlite-test.html
   ┌────────────────────────────────┐
   │ Name:  John Doe                │
   │ Email: john@example.com        │
   │ Role:  student                 │
   │ [Add User Button]              │
   └────────────────────────────────┘

2. JavaScript sends POST request
   POST http://localhost:3001/api/sqlite/users
   Content-Type: application/json
   Body: {
     "name": "John Doe",
     "email": "john@example.com",
     "role": "student"
   }

3. Express route handler receives request
   app.post('/api/sqlite/users', async (req, res) => {
     // Validate input
     // Call model method
   })

4. SQLiteUser.addUser() executes
   • Validates role
   • Checks required fields
   • Executes SQL INSERT
   • Returns created user

5. SQL Query executed
   INSERT INTO users (name, email, role)
   VALUES ('John Doe', 'john@example.com', 'student')

6. Database returns new user
   {
     id: 1,
     name: "John Doe",
     email: "john@example.com",
     role: "student",
     created_at: "2025-10-13 14:26:34"
   }

7. Express sends JSON response
   HTTP 201 Created
   {
     "success": true,
     "user": { ... }
   }

8. Frontend displays result
   ┌────────────────────────────────┐
   │ ✅ User Added Successfully     │
   │ ID: 1                          │
   │ Name: John Doe                 │
   │ Email: john@example.com        │
   │ Role: student                  │
   └────────────────────────────────┘
```

---

## Error Handling Flow

```
User Input
    │
    ▼
Validation Layer
    │
    ├─ Valid ────────────────┐
    │                        │
    └─ Invalid               │
         │                   │
         ▼                   ▼
    Return Error      Process Request
         │                   │
         │                   ▼
         │            Database Layer
         │                   │
         │            ├─ Success ──────┐
         │            │                │
         │            └─ Error         │
         │                 │           │
         ▼                 ▼           ▼
    ┌─────────────────────────────────────┐
    │        Error Response                │
    │  • 400 Bad Request                  │
    │  • 404 Not Found                    │
    │  • 500 Server Error                 │
    └─────────────────────────────────────┘
                    │
                    ▼
            Display to User
```

---

## Technology Stack

```
┌────────────────────────────────────────┐
│          Frontend Layer                 │
│  • HTML5                               │
│  • CSS3 (Modern styling)               │
│  • Vanilla JavaScript (Fetch API)      │
└────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│          Backend Layer                  │
│  • Node.js (Runtime)                   │
│  • Express.js (Web framework)          │
│  • SQLite3 (Database driver)           │
└────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│         Database Layer                  │
│  • SQLite 3 (Database engine)          │
│  • File-based storage                  │
│  • ACID compliance                     │
└────────────────────────────────────────┘
```

---

## Scalability Path

```
Current: SQLite (Single file)
    │
    ├─ Good for: Development, demos, small apps
    │
    ▼
Future: PostgreSQL/MySQL (Client-server)
    │
    ├─ Good for: Production, high concurrency
    │
    ▼
Advanced: Distributed database
    │
    └─ Good for: Large scale, global apps
```

---

## Security Layers

```
┌────────────────────────────────────────┐
│     Input Validation Layer              │
│  • Required field checks               │
│  • Email format validation             │
│  • Role enumeration check              │
└────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│     SQL Injection Protection            │
│  • Parameterized queries               │
│  • No string concatenation             │
│  • Prepared statements                 │
└────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│     Database Constraints                │
│  • UNIQUE email constraint             │
│  • NOT NULL checks                     │
│  • CHECK constraints on role           │
└────────────────────────────────────────┘
```

---

## Performance Characteristics

```
Operation          | Time      | Notes
─────────────────────────────────────────────
Database Init      | ~50ms     | One-time on startup
Add User          | <5ms      | Single INSERT
Get All Users     | <10ms     | SELECT * (cached)
Search Users      | <15ms     | LIKE query
Get Statistics    | <20ms     | GROUP BY query
Update User       | <5ms      | Single UPDATE
Delete User       | <5ms      | Single DELETE
```

---

This architecture demonstrates a clean, scalable, and maintainable approach to database integration in a Node.js application. Perfect for hackathon presentations! 🚀
